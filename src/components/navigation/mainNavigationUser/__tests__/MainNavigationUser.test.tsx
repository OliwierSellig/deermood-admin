describe('Render', () => {
  it('should render correct name and email when user is authenticated', () => {
    // Have to figure out how to render next.js async server components
  });
});

describe('Functionality', () => {
  it('should not render anything when user is not authenticated', () => {
    // Have to figure out how to render next.js async server components
  });
});
